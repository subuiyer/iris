logging:
  level: INFO
  loggers:
    com.flex66: DEBUG

# Database settings.
database:
    driverClass: org.postgresql.Driver
    user: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/iris
